import React from 'react'

// interface userData {
//     name: string,
//     age: number,
//     address: string
// }

const PropTest = (props: { fname: string | undefined, age: number, isDeleted: boolean }) => {

    // console.log('props: ', props);
    // const [name, setname] = useState<userData | any>({})
    // console.log('name: ', name);

    // useEffect(() => {

    //     setTimeout(() => {
    //         setname(true)
    //     }, 3000);
    // }, [])

    return (
        <div></div>
    )
}

export default PropTest